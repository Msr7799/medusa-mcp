# Medusa Admin MCP Server (Vercel Ready)

سيرفر MCP جاهز للعمل على بيئة **Vercel**، يوفر أدوات ذكية للتحكم بمتجر **Medusa v2** الخاص بك عبر الذكاء الاصطناعي (مثل Claude Desktop أو Cursor).

## ✨ المميزات

- **جاهز لـ Vercel**: إعدادات `vercel.json` مهيئة مسبقاً للعمل كـ Serverless Function.
- **آمن (Safe Writes)**: عمليات التعديل والحذف تتطلب خطوة تأكيد إضافية.
- **SSE Support**: يدعم Server-Sent Events للاتصال المستقر.

---

## 🚀 النشر على Vercel (Deployment)

1. **ارفع الكود إلى GitHub**.
2. **استورد المشروع في Vercel**.
3. **أضف المتغيرات البيئية (Environment Variables)**:

| المتغير | الوصف | مثال |
|---------|-------|------|
| `MEDUSA_BASE_URL` | رابط Medusa Backend | `https://your-medusa.koyeb.app` |
| `MEDUSA_AUTH_TOKEN` | مفتاح API للأدمن | `sk_123...` |
| `MEDUSA_AUTH_MODE` | وضع المصادقة | `basic` |
| `CONFIRM_SECRET` | كلمة سرية طويلة | `random_string` |
| `REQUIRE_CONFIRM` | تفعيل التأكيد | `true` |

---

## 🔌 طريقة الاستخدام (Connecting to Clients)

رابط السيرفر الخاص بك سيكون:
`https://YOUR-PROJECT.vercel.app/mcp`

### ✅ الطريقة الصحيحة (Claude Desktop & Cursor)

أضف الإعدادات التالية في ملف التكوين (`claude_desktop_config.json` أو في إعدادات MCP في Cursor):

```json
{
  "mcpServers": {
    "MedusaAdmin": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "https://YOUR-PROJECT.vercel.app/mcp"
      ]
    }
  }
}
```

> **ملاحظة:** نستخدم `mcp-remote` كجسر للاتصال بالسيرفر البعيد. تأكد من كتابة الرابط مع `https://`.

---

## ⚠️ ملاحظات هامة

- تأكد من أن رابط Medusa يعمل (`MEDUSA_BASE_URL`).
- تأكد من صحة `MEDUSA_AUTH_TOKEN`.
- إذا واجهت مشاكل في الاتصال، تأكد من تحديث ملف `src/index.ts` ليدعم `GET` requests (تم التحديث في أحدث نسخة).
