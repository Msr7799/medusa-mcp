# Medusa Admin MCP Server (Vercel Ready)

سيرفر MCP جاهز للعمل على بيئة **Vercel**، يوفر أدوات ذكية للتحكم بمتجر **Medusa v2** الخاص بك عبر الذكاء الاصطناعي (مثل Claude Desktop أو Cursor).

يتميز السيرفر بالأمان العالي، حيث تتطلب عمليات الكتابة (Create, Update, Delete) تأكيداً (Confirmation) قبل التنفيذ لحماية متجرك من أي تغييرات غير مقصودة.

## ✨ المميزات

- **جاهز لـ Vercel**: إعدادات `vercel.json` مهيئة مسبقاً للعمل كـ Serverless Function.
- **آمن (Safe Writes)**: عمليات التعديل والحذف تتطلب خطوة تأكيد إضافية.
- **أدوات شاملة**:
  - `admin_list_products`, `admin_get_product`: عرض المنتجات.
  - `admin_create_product`, `admin_update_product`: إدارة المنتجات.
  - `admin_upload_file_base64`: رفع الصور والملفات.
  - وغيرها من الأدوات المساعدة.

---

## 🚀 النشر على Vercel (Deployment)

1. **ارفع الكود إلى GitHub**.
2. **انتقل إلى [Vercel](https://vercel.com) وأضف مشروع جديد (Add New Project)**.
3. **استورد المستودع (Import Repository)** الخاص بهذا المشروع.
4. **في إعدادات المتغيرات البيئية (Environment Variables)، أضف القيم التالية**:

| المتغير | الوصف | مثال |
|---------|-------|------|
| `MEDUSA_BASE_URL` | رابط الباك-إند الخاص بـ Medusa | `https://your-store-backend.koyeb.app` |
| `MEDUSA_AUTH_TOKEN` | مفتاح الـ API السري الخاص بالأدمن | `sk_123...` (Secret API Key) |
| `MEDUSA_AUTH_MODE` | وضع المصادقة (اتركه `basic`) | `basic` |
| `CONFIRM_SECRET` | كلمة سرية طويلة لتوقيع رموز التأكيد | `any_long_random_string` |
| `REQUIRE_CONFIRM` | تفعيل أمان الكتابة (ينصح بـ true) | `true` |

1. **اضغط Deploy**.

---

## 🔌 طريقة الاستخدام (Connecting to Clients)

بعد النشر، سيكون رابط السيرفر الخاص بك:
`https://YOUR-PROJECT.vercel.app/mcp`

### مع Claude Desktop

أضف الإعدادات التالية في ملف `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "MedusaAdmin": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sse-client",
        "https://YOUR-PROJECT.vercel.app/mcp"
      ]
    }
  }
}
```

*ملاحظة: إذا لم يعمل `server-sse-client`، يمكن استخدام أدوات اتصال أخرى تدعم HTTP MCP.*

### مع Cursor (أو أي عميل يدعم SSE/Stdio Bridge)

يمكنك استخدام أي جسر (bridge) يحول HTTP SSE إلى Stdio إذا لزم الأمر، ولكن الطريقة الأسهل هي استخدام العميل المباشر إذا كان مدعوماً.

---

## 🛠️ التطوير المحلي (Local Development)

1. انسخ ملف المتغيرات:

   ```bash
   cp .env.example .env
   ```

2. املأ البيانات في ملف `.env`.
3. ثبت المكاتب وشغل السيرفر:

   ```bash
   npm install
   npm run dev
   ```

4. السيرفر سيعمل على `http://localhost:3000/mcp`.

---

## ⚠️ ملاحظات أمنية

- **`MEDUSA_AUTH_TOKEN`**: هذا مفتاح حساس جداً (Admin Privileges). لا تشاركه أبداً ولا تضعه في كود الواجهة الأمامية (Frontend).
- **`REQUIRE_CONFIRM`**: اترك هذا الخيار `true` في البيئة الحية (Production) لتجنب قيام الذكاء الاصطناعي بحذف أو تعديل بيانات عن طريق الخطأ.
